<form method="post" action="{{ path('app_walk_registration_delete', {'id': walk_registration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ walk_registration.id) }}">
    <button class="btn">Delete</button>
</form>

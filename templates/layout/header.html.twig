<header>
	<nav>
		<div class="logo">
			<a href="{{ path('app_home') }}">
				<img src="{{ asset('images/logos/logoRVB-inline.png') }}" alt="Logo de Snif & Go">
			</a>

            <p>Plus on est de chiens, plus on rit !</p>
		</div>

        <div>
        
            <ul>
                <li><a href="{{ path('app_home') }}">Accueil</a></li>
                {% if is_granted('ROLE_USER') %}
                <li><a href="{{ path('app_profile') }}">Mon profil</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{ path('app_admin') }}">Espace Administrateur</a></li>
                {% endif %}
                <li><a href="{{ path('app_logout') }}">Deconnexion</a></li>

                {% else %}
                <li><a href="{{ path('app_login') }}">Se connecter</a></li>
                <li><a href="{{ path('app_register') }}">S'inscrire</a></li>
                {% endif %}
            </ul>
{# Emplacement du toggle Eco #}
        </div>
	</nav>

</header>
